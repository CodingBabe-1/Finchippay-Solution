"use client"; import { useState } from "react"; import { useContacts } from "@/hooks/useContacts"; interface Props { isOpen: boolean; onClose: () => void; onSelect: (publicKey: string, name: string, memo?: string) => void; } export default function ContactPicker({ isOpen, onClose, onSelect }: Props) { const { contacts } = useContacts(); const [query, setQuery] = useState(""); if (!isOpen) return null; const filtered = query ? contacts.filter(c => c.name.toLowerCase().includes(query.toLowerCase()) || c.publicKey.toLowerCase().includes(query.toLowerCase())) : contacts; return ( <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4"><div className="w-full max-w-md rounded-2xl bg-slate-900 p-6 border border-white/10"><h3 className="text-lg font-bold text-white mb-4">Select Contact</h3><input type="text" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search contacts..." className="w-full px-3 py-2 rounded-lg bg-white/5 border border-white/10 text-white text-sm mb-4" autoFocus/><div className="max-h-60 overflow-y-auto">{filtered.map(c=><button key={c.id} onClick={()=>{onSelect(c.publicKey, c.name, c.memo); onClose();}} className="w-full text-left px-3 py-2 rounded-lg hover:bg-white/5 text-sm"><span className="text-white">{c.name}</span><span className="text-xs text-slate-400 ml-2">{c.publicKey.slice(0,12)}...</span></button>)}</div><button onClick={onClose} className="mt-4 w-full rounded-xl border border-white/10 py-2 text-sm text-white">Cancel</button></div></div>); }
